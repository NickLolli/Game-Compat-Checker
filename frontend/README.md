# Frontend (React)

Not started yet — Week 5-6 of the roadmap.

Planned setup:
```bash
npx create-vite@latest . -- --template react
npm install
npm run dev
```

Planned pages/components:
- Search bar (autocomplete against Steam's game list)
- Results card (verdict + bottleneck + upgrade suggestion)
- "Build your PC" form (dropdowns for CPU/GPU/RAM sourced from backend/app/benchmarks.py)
